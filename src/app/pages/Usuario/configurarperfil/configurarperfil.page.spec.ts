import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ConfigurarperfilPage } from './configurarperfil.page';

describe('ConfigurarperfilPage', () => {
  let component: ConfigurarperfilPage;
  let fixture: ComponentFixture<ConfigurarperfilPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ConfigurarperfilPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
